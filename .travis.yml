language: rust
sudo: required

os:
  - linux
  - osx

cache: cargo
rust:
  - stable
  - beta
  - nightly

env:
  matrix:
    - FEATURE=accelerate
    - FEATURE=netlib
    - FEATURE=openblas

  global:
    - secure: CAsWkX68mwPcXVNOiBaekoq8lMWgKRhVVyNhmT2ruMpAbeTmnDLzs9IFWOjvytacMv/YH8Z85HedWxIcg7m6gCIdpIOeNqlDUvGi3T18UdIu55pp6ScOcCyHd762OsC1UGOCudVIBDwK1a8gm6D/6xzzrbxPT3Bs99HXF/vL+E8=

matrix:
  exclude:
    - os: linux
      env: FEATURE=accelerate

before_install:
  - curl https://stainless-steel.github.io/travis/fortran.sh | bash

script:
  - cargo test -vv --no-default-features --features=$FEATURE
  - if [ "$TRAVIS_RUST_VERSION" == "nightly" ]; then
      cargo bench -vv --no-default-features --features=$FEATURE;
    fi

after_success:
  - if [ "$TRAVIS_OS_NAME" == "osx" ] && [ "$FEATURE" == "openblas" ]; then
      curl https://stainless-steel.github.io/travis/documentation.sh | bash;
    fi

addons:
  apt:
    sources:
      - kubuntu-backports

    packages:
      - cmake
      - gfortran

notifications:
  email: false
